# CRA auto-loads this during `yarn test` (NODE_ENV=test). Not a secret —
# just a fixture value so Transcriber.test.jsx can assert the X-API-Key
# header is actually sent. Safe to commit.
REACT_APP_API_KEY=test-fixture-api-key
REACT_APP_BACKEND_URL=http://localhost:8000
